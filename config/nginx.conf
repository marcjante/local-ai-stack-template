# nginx.conf
# Reverse proxy mínimo: expone todo el stack en el puerto 80, repartiendo
# por ruta hacia el panel de control y el backend. Ajusta las rutas/puertos
# si cambias services.yaml.

events {}

http {
  server {
    listen 80;

    # Panel de control en la raíz
    location / {
      proxy_pass http://127.0.0.1:8090;
      proxy_set_header Host $host;
    }

    # API del backend bajo /api/
    location /api/ {
      proxy_pass http://127.0.0.1:8080/;
      proxy_set_header Host $host;
    }
  }

  # --- TLS real (🔴4 del roadmap) ---
  # Descomenta y ajusta cuando tengas certificados (Let's Encrypt/Caddy/
  # los que use tu infra). Nginx no genera certificados por sí solo — esto
  # NO se puede "simular" en código, hace falta un dominio real y un CA.
  #
  # server {
  #   listen 443 ssl;
  #   ssl_certificate     /etc/nginx/certs/fullchain.pem;
  #   ssl_certificate_key /etc/nginx/certs/privkey.pem;
  #
  #   location / {
  #     proxy_pass http://127.0.0.1:8090;
  #     proxy_set_header Host $host;
  #   }
  #   location /api/ {
  #     proxy_pass http://127.0.0.1:8080/;
  #     proxy_set_header Host $host;
  #   }
  # }
  #
  # server {
  #   listen 80;
  #   server_name _;
  #   return 301 https://$host$request_uri;   # redirige todo a HTTPS
  # }
}
